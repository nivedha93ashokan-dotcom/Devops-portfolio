apiVersion: v1
kind: Service

metadata:
  name: devops-demo-service

spec:
  type: NodePort

  selector:
    app: devops-demo-app

  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30081

